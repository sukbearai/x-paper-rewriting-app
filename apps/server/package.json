{
  "name": "server",
  "scripts": {
    "dev:start": "edgeone pages dev",
    "deploy": "rm -rf ./node_modules && edgeone pages deploy ./ -n paper-rewriting-app-server -t ",
    "sync-docs": "node scripts/sync-docs.js",
    "build": "npm run sync-docs"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "hono": "^4.7.10",
    "marked": "^15.0.12",
    "marked-highlight": "^2.2.1"
  },
  "devDependencies": {
    "@edgeone/ef-types": "latest",
    "edgeone": "latest"
  }
}
